# Investment Committee Copilot Updates

Public update channel for the Investment Committee Copilot application.

This repository contains application update packages and update metadata only. It must never contain client data, portfolio records, reports, uploaded client documents, or API keys.

The app checks `update_manifest.json` for the current stable release.
